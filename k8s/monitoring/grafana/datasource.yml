apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-datasources
  namespace: smartbank
data:
  prometheus.yml: |
    apiVersion: 1
    datasources:
      - name: Prometheus
        type: prometheus
        url: http://prometheus-service:9090
        access: proxy
        isDefault: true
        editable: true
